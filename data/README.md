# Sample Documents

This directory contains example Markdown documents used for local indexing demos.

Do not commit real internal policies, customer data, credentials, private logs, or generated Qdrant storage.
`data/qdrant_storage/` is intentionally ignored and should be recreated locally by running `./scripts/ingest.sh`.
